<!--- Web.stories.mdx -->

import { Button, Space, Text } from "../lib/utente";
import { Story, Canvas, Meta } from "@storybook/addon-docs";
import { RedditDemo } from "./examples/RedditDemo/RedditDemo.js";
import { RedditCommentDemo } from "./examples/RedditCommentDemo/RedditCommentDemo.js";
import { RedditPageDemo } from "./examples/RedditPageDemo/RedditPageDemo.js";

<Meta title="Web" />

# Web Examples

Here we want to provide some examples of how you can use the library.

## 1. Reddit

Some of the basic components such as `Space`, `List`, `Button`, `Text` allow us to customize further using `style`.

<Canvas>
  <Story
    story={RedditDemo}
    decorators={[
      storyFn => (
        <div
          style={{
            transform: "scale(1)",
            height: "100vh"
          }}
        >
          {storyFn()}
        </div>
      )
    ]}
  />
</Canvas>

## 2. RedditPage

Some of the basic components such as `Space`, `List`, `Button`, `Text` allow us to customize further using `style`.

<Canvas>
  <Story
    story={RedditPageDemo}
    decorators={[
      storyFn => (
        <div
          style={{
            transform: "scale(1)",
            height: "100vh"
          }}
        >
          {storyFn()}
        </div>
      )
    ]}
  />
</Canvas>

## 3. RedditComment

<Canvas>
  <Story
    story={RedditCommentDemo}
    decorators={[
      storyFn => (
        <div
          style={{
            transform: "scale(1)",
            height: "100vh"
          }}
        >
          {storyFn()}
        </div>
      )
    ]}
  />
</Canvas>
